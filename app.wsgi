import sys
import os

# Agregar la ruta a la carpeta donde está tu app.py
sys.path.insert(0, "C:/xampp/htdocs/Desarrollo TV Beta")

from app import app as application  # Cambia 'app' por el nombre de la variable de tu aplicación Flask
